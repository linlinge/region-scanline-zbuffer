#include <iostream>
#include "my_opengl.h"
#include <string>
#include "load_obj.h"
using namespace std;
float scale = 1.0f;
Vec3f light0(100, 100, 500);

int main(int argc, char** argv)
{
	OpenglFunc(argc, argv);
	return 0;
}